mutation sntPerson($document: String!) {
    sntPerson(document:$document)
    {
        success
        status
        message
        person{
            sntNames
            sntAddress
        }
    }
}