query SearchGeographicLocationQuery($search: String!) {
    searchGeographicLocationCode(search: $search) {
        districtId
        districtDescription
        provinceDescription
        departmentDescription
    }
}
